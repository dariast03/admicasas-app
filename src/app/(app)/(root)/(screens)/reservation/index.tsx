import DefaultLayout from "@/layout/DefaultLayout";
import React from "react";
import { View, Text } from "react-native";

const Reservation = () => {
  return (
    <DefaultLayout>
      <View>
        <Text>Texto de datos</Text>
      </View>
    </DefaultLayout>
  );
};

export default Reservation;
